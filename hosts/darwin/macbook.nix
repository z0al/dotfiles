{
  d.profiles = {
    dev = {
      enable = true;
      typescript.enable = true;
      python.enable = true;
      rust.enable = true;
    };
  };

  d.programs = {
    obsidian.enable = true;
  };
}
